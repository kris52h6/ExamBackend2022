#spring.jpa.hibernate.ddl-auto=create

spring.datasource.url=${JDBC_DATABASE_URL}
spring.datasource.username=${JDBC_USERNAME}
spring.datasource.password=${JDBC_PASSWORD}
#JDBC_DATABASE_URL=jdbc:mysql://localhost:3306/3SEM_exam;JDBC_USERNAME=dev;JDBC_PASSWORD=ax2
spring.jpa.show-sql=true

server.error.include-stacktrace=never
#min backend ville ikke kaste fejlbeskeder fra ResponseStatusExceptions, så var nødt til at tilføje følgende:
server.error.include-message=always
#logging.level.root=debug

#Comment this out if you plan to use a real DB
#spring.h2.console.enabled=true
#spring.datasource.url=jdbc:h2:mem:testdb
